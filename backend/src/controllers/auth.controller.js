export const signup = (_, res) => {
    res.send("this signUp controller")
}
export const login = (_, res) => {
    res.send("this login controller")
}
export const logout = (_, res) => {
    res.send("this logout controller")
}
